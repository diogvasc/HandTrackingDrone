To run this project it is nedeed to have codes running:

  - Video Controller
  - Flight Controller

The video controller runs on a PC
The Flight Controller runs on an ESP32 connected to the drone controller
